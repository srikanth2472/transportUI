import { ThousandPipePipe } from './thousand-pipe.pipe';

describe('ThousandPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ThousandPipePipe();
    expect(pipe).toBeTruthy();
  });
});
